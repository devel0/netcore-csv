# PropNameHeaderMapping property (SearchAThing.CSV.CsvOptions)
custom dictionary to map property name to csv header

## Signature
```csharp
public Dictionary<string, string> PropNameHeaderMapping
{
    get;
    set;
}
```
